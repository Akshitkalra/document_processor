# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Document extraction ---
PyMuPDF==1.25.1            # fast PDF text + image extraction (fitz)
pdfplumber==0.11.4         # table-aware text fallback
python-docx==1.1.2         # .docx parsing
Pillow==11.1.0             # image handling for OCR
pytesseract==0.3.13        # Tesseract OCR bindings

# --- Embeddings + vector DB ---
sentence-transformers==3.3.1
pinecone==5.4.2            # Pinecone vector database (v5 SDK)

# --- LLM (summary + chat) ---
google-genai>=1.0.0       # Google Gemini SDK (from google import genai)

# --- Utilities ---
tiktoken==0.8.0            # token-aware chunking helper (counting only)
tenacity==9.0.0            # retry helpers
numpy==2.2.1
